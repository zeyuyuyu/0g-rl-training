# Core
torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0
peft>=0.10.0
accelerate>=0.30.0

# veRL framework
# pip install verl  (install separately, needs specific CUDA version)

# Rollout engine (install separately based on your CUDA)
# pip install vllm==0.8.2

# Data processing
pyarrow>=15.0.0
pandas>=2.0.0
tqdm>=4.65.0

# Reward function sandbox
# (uses stdlib: subprocess, tempfile, ast — no extra deps)

# Evaluation
numpy>=1.24.0
matplotlib>=3.7.0

# 0G export: accurate keccak256 hashing
pycryptodome>=3.20.0

# Testing
pytest>=7.0.0
